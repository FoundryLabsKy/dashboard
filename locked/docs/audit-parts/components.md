# Audit — Components (raw findings)

| Location | Spec rule | Severity | Current behavior | Proposed fix | Risk |
|---|---|---|---|---|---|
| All bottom sheets — ExerciseDetailModal L2271–2276, Add-Block L3966–3968, day-picker L4366–4368, swap sheet L8666–8670, voice sheet L13843–13845, PlateCalc L14135–14137, cardio modal L14375–14382, ConvertToSplit L4735/4758 (~10 sheets) | §10 sheets: "grabber visible, pull-to-dismiss with projection-based dismissal"; §11 #7 | Critical | Zero sheets support pull-to-dismiss. Dismissal is tap-outside backdrop and/or a custom "×" text button. 5 sheets render a grabber pill that is purely decorative (L3968, L4368, L13845, L14137, paywall L551); 5 sheets have no grabber at all. The grabber affordance promises a drag that does not exist | Add a shared sheet wrapper: grabber on every sheet, drag-to-dismiss with projection threshold, keep backdrop tap as secondary. Remove "×" buttons or keep as accessibility fallback | Structural — ask first |
| 14 `alert()` calls: L3020, L3931, L3944, L3992, L7300, L8475, L8490, L10923, L10938, L13463, L13716, L13736, L14251, L14262 | §10: errors say what happened and how to fix it; §11 #7 (native browser chrome) | High | Errors, confirmations, and even success messages use browser `alert()` ("Error adding block. Please try again.", "Data synced!", "Enter activity name") — jarring OS dialog, no retry affordance, breaks the Apple illusion entirely | Route all through the existing toast system (L14632 pattern / `window.LOCKED.toast` L1443) with specific copy; validation errors become inline field messages | Safe |
| Error copy census: "Connection issue. Try again." ×8 (L4103, 4121, 7465, 7483, 9411, 9428, 9546), "Try again." as entire AI reply L12395, ErrorBoundary "Something went wrong" L1374 / L1390, "Could not get recommendation." L3944; only ONE real retry button in the app (PhotoTab L4227) | §10: errors say what happened and how to fix, no vagueness | High | ~78 catch blocks funnel into generic strings. Failures never distinguish offline vs server vs parse error, and all but one dead-end with no retry action | Standardize: "{what failed}. Check your connection and retry." plus a retry button that re-invokes the failed call (pattern exists at L4227 — reuse it) | Safe |
| Button labels ALL-CAPS, systemic: ~40+ literals incl. SAVE SPLIT L2910/4226/4273, CANCEL/DONE L2964–2965, FINISH L3588, CREATE EXERCISE L2494, SAVE WORKOUT L4659, SAVE PR L6399, SAVE MY PROGRAM L7619, LOG THIS MEAL L8773, ADD TO TRAIN TAB L13073, START TRAINING L14005, CREATE/CANCEL L14381–14382; +8 `textTransform:"uppercase"` (292 all-caps strings file-wide incl. headers) | §10: copy is sentence case, plain verbs; brand: "the interface never shouts" | High | Primary CTAs shout in caps + Barlow Condensed 800, directly against both spec and brand voice; secondary buttons on the same screens are mixed case | Global copy pass: "Save split", "Finish workout", "Log this meal". Mechanical find/replace on button literals | Safe |
| Empty states without an action: history "No workouts logged yet" L4493, "No workouts in the last 7 days" L6515, PR Vault "No PRs yet" L6554, Goals L6022–6027, My Store L9132–9136, stores L9899–9903, supplements L11452, coach chat L13060 | §10: empty state = an invitation to act, one clear action | High | 8 of ~12 empty states are plain muted text with zero tap target. Good counter-examples exist: TrainHub splits L4437–4448 and WorkoutLog L3593 | Give each a single button wired to the screen's existing add/create flow; coach chat gets 2–3 starter prompt chips | Safe |
| Exclamation marks in UI copy ×14: L975, L4096/4114, L7151, L7300, L7458/7476, L8490, L10938, L12147, L12531, L12549, L12586, L12640, L13463 | Brand: "No hype, no exclamation marks" | Medium | "Data synced!", "Split saved to Train tab!", "Your program is ready!", "Password updated!" — fake enthusiasm on routine confirmations | Drop the marks | Safe |
| Split cards, TrainHub L4473–4483 | §2 one primary action per card; §10 lists: swipe actions | Medium | Every split card carries a permanent 3-button row (Start / Edit / Delete) at equal weight; Delete (L4480) destroys the split instantly with no confirmation and no undo | Keep Start as the sole visible action; move Edit/Delete behind swipe actions or a long-press menu; at minimum add a confirm step to Delete (confirm alone is Safe) | Structural — ask first |
| Nav active remap, L14626 vs routes L14574/14579 | §10 navigation: tab bar reflects the active section | Medium | Remap covers workout→train, settings/betaAdmin→profile, progress→home, but NOT cardio or shopping — on those screens no tab is highlighted | Extend the ternary: `cardio→"train"`, `shopping→"fuel"` | Safe |
| Inconsistent button vocabulary/casing: Title Case "Build Manually" L4443, "Start Fresh"/"Resume" L14597–14598, "Add Custom Store" L9895 vs sentence "Try again" L4227 vs CAPS; same action varies: DONE L2965 vs FINISH L3588 vs SAVE L4857; ADD L8879 vs ADD TO LIST L10183 vs LOG ENTRY L5793 vs LOG THIS MEAL L8773 | §10: same word for the same action everywhere | Medium | Three casing conventions coexist, and save/log/add/done vocabulary is shuffled per screen | Pick one verb per action app-wide (save, log, add, done) in sentence case; fold into the caps copy pass | Safe |
| Three parallel toast systems: vanilla shell toast L994–1006, React save toast "✓ Saved" L14632–14636, imperative DOM voice toast L14630/L13634, cardio "✓ LOGGED" L14637–14640, plus per-screen `setSavedMsg` timers (L12147, L12531…) | §10 consistency; brand "Organized" | Medium | Same event class renders 4 visually different ways — different colors, animations, positions, casing | One toast component/API, one style; route vanilla shell + voice + cardio through it | Safe |
| `screen==="shopping"` route, L14579 vs header guard L11137–11141 | §10 navigation: secondary screens reachable with back top-left | Medium | Route table instantiates ShoppingBudgetTab without `onBack`, so the back button doesn't render — a dead-end screen with no highlighted nav tab (latent: only live entry is Fuel's inline `view==="shop"` L8222 which passes onBack) | Pass `onBack:go("fuel")` at L14579, or delete the dead route | Safe |
| App-wide back navigation (Progress L6204, Settings L7075, PRHub L6377, etc.) | §10: back always top-left **plus edge swipe** | Low | Back buttons exist top-left (good) but there is no left-edge swipe-back anywhere; screen changes are instant state swaps | Add an edge-swipe handler on pushed screens mapping drag progress to a slide transition; projection decides commit | Structural — ask first |
| Context menus: none in app; `onContextMenu` actively suppressed L3773; secondary options are always-visible button rows (split cards L4473, tools menu L3574) | §10: long press reveals a context menu with preview | Low | Long press is reserved for reorder-drag only; all secondary actions live as persistent buttons, adding chrome to every card | Where cards have 2+ secondary actions, consolidate into a long-press menu; keep reorder long-press on the handle only | Structural — ask first |
| Search: 10 custom inputs all top-of-content (ExLib L2516/2582, ReplacePanel L2623, PRHub L6549, Add Lift L6183, SearchTab L9183, presets L11398/11960) | §10: system search bar placement, often bottom-aligned | Low | Custom-styled borderless inputs at the top of tall screens — farthest from the thumb; styling varies | Standardize one search-field component; consider bottom-aligned placement in the heaviest screens | Structural — ask first |
| List styling app-wide (TrainHub history L4494–4504, ExLib rows L2381, stores/supps lists) | §10: inset grouped style, swipe actions, pull-to-refresh | Low | Lists are individual floating cards; swipe actions exist only on workout set rows; no pull-to-refresh anywhere | Acceptable as a deliberate card language, but add swipe-to-delete where a red Delete button sits, and pull-to-refresh on synced lists | Structural — ask first |
| Emoji as icons: 🎯 Goals empty state L6024, 🛒/💰 Shop header badges L11146/11152, RatingRow emoji | §9 SF-Symbols-style icons; brand "not gamified" | Low | Emoji glyphs sit beside the app's stroke-icon system, reading as toy-like against the `Ic`/`D` icon set | Replace with existing `D.*` icons at matching weight | Safe |

**Summary:** Nav is a compliant 5-tab bottom bar (Home/Train/Fuel/Coach/Profile, L2326–2331) over a single state-string router; secondary screens mostly get a top-left back button but there is no edge-swipe, and cardio/shopping leave no tab highlighted.
Sheet inventory: ~10 bottom sheets/modals — 5 with decorative grabbers, 0 with pull-to-dismiss; every one closes by backdrop tap and/or a custom "×", which is the category's one Critical.
Error/empty census: 78 catch blocks resolving to 14 native `alert()`s and ~10 generic "Try again" strings with exactly 1 retry button (L4227); of ~12 empty states, only 2 are true invitations with an action.
