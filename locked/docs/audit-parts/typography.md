# Audit — Typography (raw findings)

| Location | Spec rule | Severity | Current behavior | Proposed fix | Risk |
|---|---|---|---|---|---|
| index.html:83, 1113, 1171, plus 237 inline uses (e.g. 2275, 2408, 4604, 6485, 7540) | §3 "One family: SF Pro... never a second display font"; §11.8 hard don't | Critical | 'Barlow Condensed' (Google font, wghts 700/800/900) is the display face for every heading: theme token `heading:` (L83), `@import` (L1113), global `h1,h2,h3,h4,h5,h6{font-family:'Barlow Condensed'}` (L1171), and 237 inline `fontFamily:"Barlow Condensed"` declarations on titles, buttons, and stat numerals | Delete the Barlow import and all Barlow declarations; carry hierarchy with SF system stack at 34/28/22/20pt bold/semibold. Condensed headlines are load-bearing to the brand's look, so restyle deliberately | Structural |
| index.html:1113, 1118, 1120 | §3 one system family; brand identity "Typography: System type" | Critical | Body/base family is 'DM Sans' (second imported web font): `body{font-family:'DM Sans'...}` (L1118) and `input,textarea,button,select{font-family:'DM Sans'}` (L1120). Combined with Barlow and the legacy `-apple-system` token (L82, used only in ~6 pre-React splash CSS strings, e.g. L531), three families ship | Replace both Google fonts with `-apple-system, BlinkMacSystemFont, sans-serif` app-wide; drop the `@import` (also removes a render-blocking network fetch) | Structural |
| index.html: all ~1,485 fontSize declarations + body `font-size:15px` (L1118) | §3 Dynamic Type "layouts must reflow"; §11.12; §12 checklist | Critical | Zero `rem`/`em` type anywhere (0 matches); every size is a unitless px number and the root is pinned at 15px. Root/text scaling to 310% is impossible — the type system cannot scale at all, let alone reflow | Convert body to `font-size:100%` and sizes to rem via a shared type-scale constant map; test at 200–310% root scale | Structural |
| index.html:4250, 7596, 13066 (chat bubbles), 4649, 4936 (workout notes/AI text), 7541, 7561 (onboarding copy), 3593 (empty states) — systemic, ~89% of all sizes <17 | §3 "Body text floor: 17pt. Never smaller for primary reading content" | High | Primary reading content set at 13–15px: coach chat messages `fontSize:14,lineHeight:1.6`, AI insights 13 (L4997, 5910), onboarding paragraphs 14, empty-state copy 15. Only 17 declarations in the whole file use 17, and most are Barlow button labels (e.g. L4226), not body | Raise all primary reading content (chat, notes, insights, paragraphs, list titles) to 17; keep 13/12/11 only for true captions/metadata | Structural |
| index.html:1118 | §3 body floor 17pt | High | Global default `body{...font-size:15px}` makes 15px the app's inherited reading size | Set body to 17px (1.0625rem)/`font-size:100%` and let captions opt down explicitly | Structural |
| index.html: 266 letterSpacing declarations (e.g. 1171 global 0.035em on all headings, 1170 `p{letter-spacing:0.2px}`, 13964 `0.28em`, 14004 `0.18em`, 8629 `letterSpacing:4`, 3671 `0.06em`) | §3 "Tracking is automatic per size. Do not override tracking or leading except in exceptional cases" | High | Tracking manually overridden 266 times across 16 distinct values (0.01–0.28em plus a 4px value), including two global rules that re-track every `<p>` and every heading. Positive tracking on a condensed face also fights the typeface's design | Remove the two global rules and sweep the inline overrides; retain at most a single size-specific value for the intro wordmark moment (L13964) | Safe |
| index.html:1118+1120 (inputs inherit 15px), 2414, 2460 (15px), 2516, 2582, 2623 (14px) | §3 body floor; platform behavior (iOS Safari zooms inputs <16px) | High | Text inputs render at 14–15px. Besides being sub-floor user content, any focused input under 16px triggers iOS Safari's automatic viewport zoom, breaking the native feel | Minimum 16px on all `input`/`textarea`/`select`; 17px for primary entry fields (weight/reps already use larger) | Safe |
| index.html:3656, 3671, 3686, 3808, 3813, 3860 — systemic: 6× 8px, 47× 9px | §3 type scale bottoms out at Caption 2 = 11pt | High | 53 declarations at 8–9px: set-table column headers ("#/KG/REPS/RIR"), superset badges, "LAST" labels — functional data labels rendered below any iOS text style, at heavy weights to compensate | Raise 8–9px to 11px minimum (Caption 2); redesign the set-grid header spacing to accommodate | Safe |
| index.html:2861, 3460, 3654, 3672, 3763, 9867 — systemic: 11 `textOverflow:"ellipsis"` + 19 `whiteSpace:"nowrap"` | §3 "Layouts must reflow, not truncate"; §12 checklist | Medium | Primary content — exercise names, block names, note titles — is single-line `nowrap` + `ellipsis`. At any larger text size these truncate immediately; names like "Paused Hack Squat" clip even today at narrow widths | Allow 2-line wrap (`display:-webkit-box` line clamp) for names/titles; keep ellipsis only for genuine metadata like URLs (L9867) | Safe |
| index.html: 192× 14, 161× 10, 50× 18, 13× 24, 12× 26, plus 19/21/30/36/38/11.5 (e.g. 2384, 3620, 4604 area, 6508) | §3 iOS text styles 34/28/22/20/17/16/15/13/12/11 | Medium | ~470 declarations sit on off-scale sizes; 14 is the single most-used "body" size and 10 the most-used caption, neither on the scale (10 is legitimate only for tab-bar labels, L2342) | Snap during the rem migration: 14→15 or 17 by role, 10→11, 18→17 or 20, 24/26→22 or 28 | Safe |
| index.html:2562 (11px), 3907 (13px), 5593, 9557, 9570, 10189 — 85 instances of Barlow at 11–16px | §3 display face misuse (condensed faces are display-only); §3 floor | Medium | The condensed display font is also used at caption sizes — 11px Barlow in a 40px chip, 13px section eyebrows, 13px buttons — where condensed proportions plus 700–800 weight hurt legibility most | Covered by the Barlow removal; until then, ban Barlow below 20px | Safe |
| index.html: fontWeight census — 542× 700, 212× 800, 155× 600, 12× 900, only 9× 400 explicit | §3 "Weight creates hierarchy" | Medium | 97% of explicit weights are semibold-or-heavier; body, captions, badges, and titles are all bold, so weight no longer differentiates anything — hierarchy collapses and the page reads as uniformly loud | Rebase body/captions to 400, list titles 600, screen titles 700; reserve 800+ for hero numerals | Safe |
| index.html:2296–2304, 2413, 6794, 8802, 10316 + 9 `textTransform` declarations | §10 copy "sentence case"; §3 tracking | Low | Pervasive eyebrow pattern: 10–11px, weight 700–800, wide-tracked, ALL-CAPS labels (hardcoded uppercase strings plus `textTransform:"uppercase"`) — a sportswear idiom, not the iOS 13pt regular uppercase grouped-header style | Restyle section labels to Footnote 13px regular/medium, tracking default, secondaryLabel color | Safe |
| index.html:1193–1196 | §3 (attempted tracking/weight control); code health | Low | CSS attribute hacks like `span[style*='fontSize:13']{font-weight:500;letter-spacing:0.1px}` target the camelCase string `fontSize:13`, but React serializes inline styles as `font-size: 13px` — these four global typography rules are dead code (and would misfire if they did match, e.g. `fontSize:18` matching 180) | Delete the four rules; express intended weights explicitly at call sites | Safe |

**Summary:**
fontSize histogram (px, ~1,563 numeric declarations): 8:6 · 9:47 · 10:161 · 11:323 · 11.5:1 · 12:252 · 13:287 · 14:192 · 15:46 · 16:70 · **17:17** · 18:50 · 19:1 · 20:32 · 21:2 · 22:19 · 24:13 · 26:12 · 28:12 · 30:3 · 32:6 · 34:2 · 36–74:12 — 88.6% of all type is below the 17pt body floor, and mode is 11px.
Font-family census: 'Barlow Condensed' 237 inline + global h1–h6 rule (display everywhere, incl. 85 uses at 11–16px); 'DM Sans' as body + all form controls via global CSS; legacy `-apple-system/'SF Pro Display'` token (L82) used only in ~6 pre-React splash strings; `inherit` ×5 — zero screens render system type.
Dynamic Type readiness is nil: 0 rem/em units, body pinned at 15px, 266 tracking overrides, and 19 nowrap/11 ellipsis truncations on primary content mean 310% root scaling can neither happen nor reflow.
